/**
 * DOM id of the rail slot under the Top trends panel in
 * src/app/(public)/predictions/layout.tsx. A plain module, not the client
 * component's file, so the server layout imports the string itself rather
 * than a client reference to it.
 */
export const RAIL_SLOT_ID = "prediction-rail-slot";
