"use client";import {useEffect,useState} from "react";
function bytes(v:string){const p="=".repeat((4-v.length%4)%4),s=(v+p).replace(/-/g,"+").replace(/_/g,"/");return Uint8Array.from(atob(s),c=>c.charCodeAt(0))}
export function PushSettings(){const [state,setState]=useState("loading"),[message,setMessage]=useState("");useEffect(()=>{if(!("serviceWorker" in navigator)||!("PushManager" in window)){setState("unsupported");return}setState(Notification.permission)},[]);async function enable(){setMessage("");const permission=await Notification.requestPermission();setState(permission);if(permission!=="granted")return;const key=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY;if(!key){setMessage("Push has not been configured by the site operator.");return}const reg=await navigator.serviceWorker.ready;let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes(key)});const r=await fetch("/api/push-subscriptions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(sub.toJSON())});setMessage(r.ok?"Push enabled on this device.":((await r.json()).error||"Could not enable push."))}async function disable(){const reg=await navigator.serviceWorker.ready,sub=await reg.pushManager.getSubscription();if(sub){await fetch("/api/push-subscriptions",{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({endpoint:sub.endpoint})});await sub.unsubscribe()}setMessage("Push disabled on this device.");setState("default")}async function test(){const r=await fetch("/api/push-test",{method:"POST"});const data=await r.json();setMessage(r.ok?`Sent to ${data.sent} device(s).`:data.error)};return <section className="card space-y-3"><h2 className="section-heading">Browser push</h2>{state==="unsupported"?<p className="text-sm text-gray-400">Push is unavailable in this browser. On iPhone or iPad, install BetGenius to the Home Screen first. Your inbox still works.</p>:<><p className="text-sm text-gray-400">Permission is requested only when you choose Enable. Current permission: {state}.</p><div className="flex flex-wrap gap-2"><button className="btn btn-primary" onClick={enable} disabled={state==="denied"}>Enable push</button><button className="btn btn-ghost" onClick={disable}>Disable on this device</button><button className="btn btn-ghost" onClick={test} disabled={state!=="granted"}>Send test notification</button></div></>}{message&&<p className="text-sm text-brand">{message}</p>}</section>}
